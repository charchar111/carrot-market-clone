import { PrismaClient } from "@prisma/client";

export default new PrismaClient();

// async function main() {
//   prisma.user.create({ data: { email: "333@naver.com", name: "daw" } });
// }

// main();
